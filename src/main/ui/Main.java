package ui;

// Main class where the app starts
public class Main {
    public static void main(String[] args) {
        // For console version uncomment the following
        // new Scheduler();

        // For GUI version (compare feature is not done) uncomment the following
        new SchedulerGUI();
    }
}
